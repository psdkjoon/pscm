# pscm

psdk's smart copy and move CLI tool.

## Usage

```
pc <source> <destination> [-f|--force] [-j|--jobs N]
pm <source> <destination> [-f|--force] [-j|--jobs N]
```

Works on files and directories, no `-r` needed.

## Install completion

```
sudo pc --install-completion
```
